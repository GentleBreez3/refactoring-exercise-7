<h1><%= @post.title %></h1>

<p><%= @post.content %></p>

<%= link_to 'Edit', edit_post_path(@post) %>
<%= link_to "Destroy", post_path(@post), method: :delete, data: { confirm: 'Are you sure you want to delete this entry?' } %>

<h3>Comments</h3>
<%= render @post.comments %>
	<div>
	 	<%= render 'comments/form' %>
	</div>


<%= link_to 'Back', posts_path %>

